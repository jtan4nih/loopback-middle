echo LAUNCH THIS IN THE ADMINISTRATOR WINDOW!
pause

:nodist use stable && npm install -g apiconnect && npm install -g jasmine-node-debug
nodist use stable && npm install -g strongloop && npm install -g jasmine-node-debug
