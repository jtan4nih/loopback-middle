echo "Make sure you have shut down the real API services locally!!!!!!!!!!!!!!"
echo "************************************************************************"
. ~/.bash_profile && nvm i 5 && nvm use 5 && npm test
